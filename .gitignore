# macOS
.DS_Store
.AppleDouble
.Spotlight-V100
.Trashes

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Generated / local build artifacts
build/
dist/
*.bak
*.orig

# Backups created by install.py / update.py in browser profiles
# (these live in the profile, but ignore them if ever copied into the repo)
user.js.voidfox-backup-*

# Logs from the update scripts / background service
*.log

# Editor
.vscode/
.idea/
*.swp
*.swo

# NOTE: ./upstream is intentionally NOT ignored - the GitHub Action commits the
# synced Betterfox files there, and install.py reads them from the checkout.
