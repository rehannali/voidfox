/****************************************************************************
 * voidfox - overrides/firefox.js
 *
 * Overrides applied to FIREFOX ONLY (not Zen). Loaded after common.js, so
 * anything here wins over both Betterfox and overrides/common.js for Firefox.
 *
 * Put Firefox-specific tweaks here. Leave it mostly empty if you don't need
 * any - that's fine.
 ***************************************************************************/

// Show tab previews on hover (handy with many tabs):
// user_pref("browser.tabs.hoverPreview.showThumbnails", true);

// Vertical tabs (Firefox 136+):
// user_pref("sidebar.verticalTabs", true);

// Always show the bookmarks toolbar:
// user_pref("browser.toolbars.bookmarks.visibility", "always");
